{% block extends %}
    {{ "{% extends 'LarABMGeneradorBundle::layout.html.twig' %}" }}
{% endblock extends %}
{% block id '{{ entity }}' %}
    {% block body %}
        {{ "{% block contenido -%}" }}
        {{ "% embed '::portlet.html.twig' with {'titulo': 'Editar " ~  entity  ~ ": ' ~ entity} %}" }}
        {{ "        {% block cuerpo %} " }}
        <p></p>
        {{ '
            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <div class="col-sm-offset-2">
                    <input type="submit" value="Guardar" class="btn btn-success">
                </div>

            {{ form_end(edit_form) }}
    ' }}

        <div class="row">
            <div class="col-md-2 col-md-offset-8 ">
                <div class="pull-right">
                    {{ '{{ form(delete_form) }} ' }}
                </div>
            </div>

            <div class="col-md-1  ">
                <a href="{{ '{{ path("'~ entity~'") }}' }}" class="btn btn-sm btn-default">
                    Ir a la lista
                </a>
            </div>
        </div>
        {% set hide_edit, hide_delete = true, false %}

        <div class="row">
            {% include 'crud/views/others/record_actions.html.twig' %}
        </div>

    {% endblock %}
    {% endembed %}



    {{ "{% endblock contenido %}" }}
{% endblock body %}
