
{% set msg = { 'show':     'Ver mas información',
    'edit':     'Editar los datos',
    'remove':   'Eliminar el registro'
    } %}
{% set icono = {
    'show':     'glyphicon-eye-open',
    'edit':     'glyphicon-edit',
    'remove':   'glyphicon-trash'
 }%}
                            <div class="btn-group">{{ '\n' }}
                                {%- for action in record_actions %}
                                <a href="{{ "{{ path('" ~ route_name_prefix ~ "_" ~ action ~ "', { 'id': entity.id }) }}" }}" class="btn btn-xs btn-default" data-toggle="tooltip" data-placement="top" title="{{ attribute(msg,action) }}" >
                                    <span class="glyphicon {{ attribute(icono,action) }}"></span>
                                </a>{{ '\n' }}
                                {%- endfor %}
                            </div>
